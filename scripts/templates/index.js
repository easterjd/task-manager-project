const listsTemp = require('./lists')
const nav = require('./nav')
const page = require('./page')
const tasks = require('./tasks')
const users = require('./users')

module.exports = {
  listsTemp,
  nav,
  page,
  tasks,
  users
}
